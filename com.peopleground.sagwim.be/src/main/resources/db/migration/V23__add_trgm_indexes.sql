-- =====================================================
-- V23: pg_trgm 확장 활성화 (트랜잭션 가능)
-- 인덱스 생성은 V24 (CONCURRENTLY, 비트랜잭션)에서 수행한다.
-- =====================================================

CREATE EXTENSION IF NOT EXISTS pg_trgm;
