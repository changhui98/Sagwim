-- =====================================================
-- V12: p_user.nickname 유니크 제약조건 추가
-- =====================================================

-- 기존 중복 닉네임이 있을 경우 마이그레이션 실패를 방지하기 위해
-- 중복 여부를 미리 확인한 후 제약조건을 추가한다.
ALTER TABLE p_user
    ADD CONSTRAINT uk_user_nickname UNIQUE (nickname);
