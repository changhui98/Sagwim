## 개발 환경 전용 설정
spring:
  config:
    # 로컬 직접 실행 시 .env 파일에서 환경변수 로드 (Docker 실행 시에는 컨테이너 환경변수 사용)
    # be/ 하위에 .env만 참조 — 루트 .env는 build.gradle(bootRun)에서 주입하므로 여기서 중복 로드하지 않음
    import: optional:file:.env[.properties]
  flyway:
    locations: classpath:db/migration,classpath:db/migration/dev
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true
  sql:
    init:
      mode: never

## p6spy - 개발 환경에서만 활성화
decorator:
  datasource:
    p6spy:
      enable-logging: on

## 개발 로그 레벨
logging:
  level:
    root: info
    web: debug
    com.peopleground: debug
    org.flywaydb: debug
